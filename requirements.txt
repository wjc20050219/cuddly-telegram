# RiceVar-ID Python 依赖（TASK-002 输出）
# 在 ricevar conda 环境内安装：pip install -r requirements.txt
# 注意：目标解释器为环境内的 Python 3.11（environment.yml v2），而非系统自带的 Python 3.7。

# 数据处理
numpy>=1.24
pandas>=2.0
scipy>=1.10
pyarrow>=12.0          # 大规模样本×marker 矩阵的列式存储

# 可视化
matplotlib>=3.7
seaborn>=0.12
adjustText>=1.0

# 生物信息
pysam>=0.21            # BAM/VCF/FASTQ 直接读取
biopython>=1.81
scikit-allel>=1.3      # SNP 矩阵、PCA、遗传距离

# 机器学习与统计
scikit-learn>=1.3
hmmlearn>=0.3          # 论文 CNVb 方法的 HMM 平滑（TASK-004 第 4 节 Step 1，n_components=3）

# 工具链
jupyterlab>=4.0
tqdm>=4.65
requests>=2.31
rich>=13.0

# Web 系统（后期 Phase 使用）
fastapi>=0.110
uvicorn>=0.29
sqlmodel>=0.0.16
qrcode[pil]>=7.4       # DNA 指纹二维码生成

# 测试
pytest>=7.4
